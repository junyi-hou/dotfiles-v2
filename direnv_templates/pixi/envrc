set -euo pipefail

export PATH="$(pwd)/.tools/bin:$PATH"

command -v pixi >/dev/null || {
    curl -fsSL https://pixi.sh/install.sh | PIXI_NO_PATH_UPDATE=1 PIXI_BIN_DIR=$(pwd)/.tools/bin bash
}

eval "$(pixi shell-hook)"

# Local Variables:
# mode: bash-ts
# End: